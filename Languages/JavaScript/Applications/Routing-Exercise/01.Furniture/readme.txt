npm install -> to install dependencies
images don't load in details view because an error in the server data
it can be fixed by removing the dot from the img property
./images/exp.png => /images/exp.png
